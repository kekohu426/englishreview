# Deprecated Stage 1 Original Prompt

Deprecated backup. It must not be used for generation because the product flow now requires:

- Parent-confirmed homework analysis before generation.
- OPW2 knowledge plus natural phonics knowledge.
- 11 question types only.
- At least 5 tasks per type.
- No `letter_sound_trace`.
- Task traceability with `source_refs`, `knowledge_tags`, `ability_targets`, and `generation_intent`.

Use `prompts/stage1_balanced.md` instead.
