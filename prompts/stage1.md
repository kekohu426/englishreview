# Stage 1 Prompt

This prompt is intentionally aligned with `stage1_balanced.md`.
Use 11 question types only and never generate `letter_sound_trace`.

See `prompts/stage1_balanced.md` for the active production prompt.
