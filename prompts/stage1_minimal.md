# Deprecated Stage 1 Minimal Prompt

Deprecated. The app now uses confirmed homework analysis plus OPW2 and natural phonics knowledge.

Use `prompts/stage1_balanced.md`.

Required behavior:
- 11 question types only.
- Minimum 5 tasks per type.
- No `letter_sound_trace`.
- Preserve `source_refs`, `knowledge_tags`, `ability_targets`, and `generation_intent`.
